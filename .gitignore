*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/app/build
/captures
.externalNativeBuild
.cxx
*.apk

# Signing secrets — never commit these
*.jks
*.keystore
keystore.properties

# Stray typo of keystore.properties — never track
keystore.propertie
